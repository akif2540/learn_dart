void main() {
  int money = 5;
  money = money + 1;
  money += 1;
  money++;

  String name = "veli";
  String bacik = "bacik";

  print(name + bacik);
  if (name == 'veli') ;
  if (name.length > 'veli'.length) ;
  if (name.length < 'veli'.length) ;
  if (name.length <= 'veli'.length) ;
  if (name.length >= 'veli'.length) ;
  if (name != 'veli') ;
}
